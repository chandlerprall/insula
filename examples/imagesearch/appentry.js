import React from 'react';
import ReactDOM from 'react-dom';
import TodoApp from './views/ImageSearchApp';

ReactDOM.render(
    React.createElement(TodoApp),
    document.getElementById('container')
);